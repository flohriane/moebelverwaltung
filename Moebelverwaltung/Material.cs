
namespace BBW.Moebelverwaltung
{
    // Aufzählungsdatentyp, der mehrere Konstanten gruppiert
    // Enumeration
    // Konstanten sind vom Datentyp int 
    // (als Wert automatisch int-Werte beginnend ab 0 zugewiesen) 
    // Konstantennamen müssen sich an Regeln für Bezeichner halten
    /// <summary>
    /// Enum, das die Materialarten für Möbelstücke enthält
    /// und als Eigenschaftstyp in Moebelstueck verwendet wird
    /// </summary>
    public enum Material{Holz,Stahl,Glas,Kunststoff}
}
